module.exports = {
  plugins: {
    // Tailwind CSS processes our utility classes
    tailwindcss: {},
    // Autoprefixer adds vendor prefixes for better browser compatibility
    autoprefixer: {},
  },
}